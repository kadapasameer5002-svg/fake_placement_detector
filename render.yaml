services:
  - type: web
    name: fake-placement-detector
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd fake_placement_alert && gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
